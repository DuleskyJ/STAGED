<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Registry</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <header class="display-flex justify-space-between align-center p-2">
    <h1>
      <a class="neon" href="/">STAGED</a>
    </h1>

    <nav>
      {{#if logged_in}}
      <a class="neon" id="logout" href="/">Logout</a>
      {{else}}
      <a class="neon" id="login" href="/login">Login</a> <span class="neon">|</span> <a class="neon" id="signup" href="/signup">Sign-Up</a>
      {{/if}}
    </nav>

  </header>
  <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  <footer>
      
  </footer>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
